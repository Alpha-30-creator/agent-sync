# Decision log

Short ADRs: the *why*, and what we rejected. The design docs say what the system does; these say
why it does it that way, so a later session (or contributor) doesn't quietly undo a deliberate
choice. Format: Context → Decision → Consequences → Alternatives rejected.

To change a decision, write a new ADR that supersedes the old one; don't edit history.
